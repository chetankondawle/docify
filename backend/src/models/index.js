/**
 * Models index
 *
 * Export all models from here for easy importing.
 * Example:
 *   const { User } = require('../models');
 *
 * Add your ORM/ODM models here as the project grows.
 * Supports Mongoose, Sequelize, Prisma, etc.
 */

module.exports = {};
